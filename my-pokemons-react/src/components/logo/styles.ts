import styled from "styled-components";

export const Container = styled.div`
  margin: auto;
  max-height: 18vh;

  font-size: 1.3rem;
  color: var(--color-yellow);
  -webkit-text-stroke: 0.09rem var(--color-blue);
  margin: 0.75rem 7%;
`;
